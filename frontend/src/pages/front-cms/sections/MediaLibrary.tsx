import MediaBrowser from '../components/MediaBrowser';

const MediaLibrary = () => {
    return (
        <div className="h-full">
            <MediaBrowser />
        </div>
    );
};

export default MediaLibrary;
